<div class="todo-item">
    <div class="col-sm-12">
        <h3>{{ name }}</h3>
        <img src="{{image_url}}" alt="Picture of Business" height="100" width="100">
        <p>{{ alias }}</p>
    </div>
</div>